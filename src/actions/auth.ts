'use server';

import { redirect } from "next/navigation";

export async function signInWithGoogle() {
  // auth-config doesn't export signIn in v4, doing nothing
}

export async function signOutAction() {
  // Doing nothing
}
